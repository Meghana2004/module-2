#include<stdio.h>
int main()
{
    int t,r,n,sum=0;
    printf("enter your value");
    scanf("%d",&n);
    while(n>0)
    {
        t==n;
        r=n%10;
        sum=sum*10+r;
        n=n/10;
    }
    if(t==sum)
    printf("the value is palindrome");
    else
    printf("not a palindrome");
}
